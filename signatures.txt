# Format: name|pattern
# Pattern is matched as a literal (case-sensitive) byte substring against
# the application-layer payload of TCP/UDP/ICMP packets. Keep patterns
# specific enough to avoid false positives on legitimate traffic.

sql-injection-union|UNION SELECT
sql-injection-or1eq1|' OR '1'='1
path-traversal-etc-passwd|../../../../etc/passwd
xss-script-tag|<script>
cmd-exec-attempt|; cat /etc/shadow
windows-cmd-exec|cmd.exe /c
log4shell-jndi|${jndi:ldap://
webshell-marker|eval(base64_decode(
